<h1> Your Profile Info</h1>
<p><%= image_tag(current_user.profile.avatar_url, size: "50x50", class:"rounded-circle") %></p>

<p><%= current_user.profile.nickname %></p>
<p><%= current_user.profile.country %></p>

<p><%= link_to "Edit", edit_profile_path(current_user.profile.id), remote: true, data: { toggle: 'modal', 'target': '#edit-profile'} %>
<div id="edit-profile"></div>
